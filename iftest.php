<?php

$total_price="";
if (($total_price == 0) || ($total_price == "" )) {
  $remains = $total_price;
  echo " IF :".$total_price;
} else {
  $remains = $total_price;
  echo "Else :".$total_price;
}
?>
